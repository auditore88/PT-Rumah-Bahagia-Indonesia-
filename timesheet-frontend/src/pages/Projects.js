import React from 'react';
import ProjectForm from '../components/ProjectForm';

const Projects = () => {
    return (
        <div>
            <h1>Projects</h1>
            <ProjectForm />
        </div>
    );
};

export default Projects;
